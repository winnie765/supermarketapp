<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <title>Supermarket App</title>
</head>
  <body class="home-page">
  <% const isAdmin = user && user.role === 'admin'; %>
  <%- include('partials/nav', { user, showNavLinks: false }) %>

  <% if (!isAdmin) { %>
    <section class="hero container">
      <div class="hero-content">
        <p class="eyebrow">Shop</p>
        <h1>Shop for Groceries<br><span>Online Here</span></h1>
        <p class="lead">
          Fresh fruit, vegetables, and pantry staples delivered straight to your door.
          Discover weekly deals and stock up in minutes.
        </p>
        <div class="hero-actions">
          <a href="/shopping" class="btn btn-lime">Shop Now</a>
        </div>
      </div>
      <div class="hero-visual">
        <div class="hero-bubble">
          <img src="/images/homepage.png" alt="Groceries in a basket">
        </div>
      </div>
    </section>
  <% } %>
</body>
</html>
