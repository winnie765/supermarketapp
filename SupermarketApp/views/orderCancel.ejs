<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <title>Cancel Order</title>
</head>
<body class="home-page">
  <% const showSearch = false; const showNavLinks = true; %>
  <%- include('partials/nav', { user, showSearch, showNavLinks }) %>

  <div class="container py-4">
    <div class="row justify-content-center align-items-center" style="min-height: 80vh;">
      <div class="col-lg-12 col-xl-10" style="max-width: 1100px;">
        <% if (messages && messages.error && messages.error.length) { %>
          <div class="alert alert-danger"><%= messages.error.join(', ') %></div>
        <% } %>
        <% if (messages && messages.success && messages.success.length) { %>
          <div class="alert alert-success"><%= messages.success.join(', ') %></div>
        <% } %>

        <div class="card shadow-sm border-0">
          <div class="card-body p-5 p-lg-6 d-flex flex-column justify-content-center align-items-center text-center" style="min-height: 360px;">
            <h2 class="mb-3">Pending cancellation for order</h2>
            <div class="text-muted fs-4 mb-4"><%= order.invoiceNumber %></div>
            <p class="fs-5 mb-4">If you proceed, this order will be marked as cancelled.</p>
            <div class="d-flex flex-wrap gap-2 justify-content-center">
              <a href="/orders" class="btn btn-lime btn-lg pill-shape btn-xl">Back to orders</a>
              <form action="/orders/<%= order.invoiceNumber %>/cancel" method="post" onsubmit="return confirm('Cancel this order?')">
                <button type="submit" class="btn btn-danger btn-lg pill-shape btn-xl">Confirm cancel</button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
